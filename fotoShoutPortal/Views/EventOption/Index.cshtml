@model IEnumerable<FotoShoutData.Models.EventOption>

@{
    ViewBag.Title = "Event Options";
    ViewBag.AppName = "fotoShout";
}

@Html.Partial("~/Views/Preferences/_PreferCommands.cshtml", "EventOptions")

<section id="main-panel">
    <div class="div-list">
        <h2>@ViewBag.Title</h2>
        <p>
            @Html.ActionLink("New Event Option", "Create")
        </p>
        <fieldset>
            <legend>@ViewBag.Title</legend>
            <table>
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.EventOptionName)
                    </th>
                    <th></th>
                </tr>

            @foreach (var item in Model) {
                <tr class="row-item">
                    <td>
                        @Html.DisplayFor(modelItem => item.EventOptionName)
                    </td>
                    <td>
                        <p class="hidden">
                            @Html.ActionLink(" ", "Edit", new { id = item.EventOptionId }, new { @class = "linkEdit" })
                            @Html.ActionLink(" ", "Delete", new { id = item.EventOptionId }, new { @class = "linkDelete" })
                        </p>
                    </td>
                </tr>
            }

            </table>
        </fieldset>
    </div>
</section>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
    <script type="text/javascript" src="@Url.Content("~/Scripts/inc/eventoptions.js?1.3.0.10")"></script>
    <script type="text/javascript">
        $(document).ready(function () {
            var tabCtrl = new TabControl('@Request.ApplicationPath');
            var initializer = new Initializer(tabCtrl, "preferences");
            var evOptions = new EventOptions();
        });
    </script>
}

