@model IEnumerable<FotoShoutData.Models.Sponsor>

@{
    ViewBag.Title = "Event Clients";
    ViewBag.AppName = "fotoShout";
}

@Html.Partial("~/Views/Preferences/_PreferCommands.cshtml", "EventSponsors")

<section id="main-panel">
    <div class="div-list">
        <h2>@ViewBag.Title</h2>
        <p>
            @Html.ActionLink("New Event Client", "Create")
        </p>
        <fieldset>
            <legend>@ViewBag.Title</legend>
            <table>
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.SponsorName)
                    </th>
                    <th></th>
                </tr>

            @foreach (var item in Model) {
                <tr class="row-item">
                    <td>
                        @Html.DisplayFor(modelItem => item.SponsorName)
                    </td>
                    <td>
                        <p class="hidden">
                            @Html.ActionLink(" ", "Edit", new { id = item.SponsorId }, new { @class = "linkEdit" })
                            @Html.ActionLink(" ", "Delete", new { id = item.SponsorId }, new { @class = "linkDelete" })
                        </p>
                    </td>
                </tr>
            }

            </table>
        </fieldset>
    </div>
</section>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
    <script type="text/javascript" src="@Url.Content("~/Scripts/inc/eventsponsors.js?1.3.0.10")"></script>
    <script type="text/javascript">
        $(document).ready(function () {
            var tabCtrl = new TabControl('@Request.ApplicationPath');
            var initializer = new Initializer(tabCtrl, "preferences");
            var evSponsors = new EventSponsors();
        });
    </script>
}

